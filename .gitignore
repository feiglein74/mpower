__pycache__/
*.pyc

# Enthaelt das Klartext-Passwort des MQTT-Brokers. Die uebrige Geraetesicherung
# ist absichtlich versioniert -- nur diese eine Datei nicht.
backup-geraet/mqtt/client/mqtt.cfg

# Falls jemand die lokale Konfiguration doch ins Projekt legt.
config.ini

# Enthaelt mgmt.authkey und mgmt.cloud_pass des Geraets. Fuer ein Rollback
# lokal noetig, gehoert aber nicht in ein veroeffentlichtes Repository.
# Die bereinigte Struktur steht in cfg/mgmt.beispiel.
backup-geraet/cfg/mgmt

# Verbrauchshistorie des Geraets (tageweise, ab August 2020). Lokal Teil der
# Sicherung, aber personenbezogen -- daraus lassen sich Anwesenheitszeiten und
# Tagesablaeufe ablesen. Gehoert nicht in ein oeffentliches Repository.
backup-geraet/data/

# Hersteller-Datenblatt, nicht unser Werk -- Urheberrecht liegt bei Prolific.
# Bezugsquelle steht im README.
PL7223.PDF
